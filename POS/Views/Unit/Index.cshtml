@*@model IEnumerable<POS.Models.Unit>

@{
    ViewData["Title"] = "Index";
}



<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Unit List</li>
                    </ol>
                </nav>
                <br /><br />
                <div class="row">
                    <div class="col-md-12 table-responsive-md">
                        <a class="btn btn-success" asp-controller="Unit" asp-action="Create"><i class="fa fa-plus"></i>&nbsp;Add</a><br />
                        <br />
                        <table class="table table-striped">
                            <thead>
                                <tr style="background-color:burlywood;margin-left:50px">
                                    <th>Unit Name</th>                                    
                                    <th class="text-warning">Action</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                                                                  
                                        <td>@Html.DisplayFor(i => item.Name)</td>                                        
                                        <td>
                                            <a class="text-primary btn btn-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="fa fa-edit"></i></a>                                           
                                            <a class="text-danger btn btn-danger" asp-action="Delete" asp-route-id="@item.Id"><i class="fa fa-trash"></i></a>
                                        </td>
                                        <td></td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>*@





